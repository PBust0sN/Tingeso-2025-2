apiVersion: v1
kind: Service
metadata:
  name: ms-db-kardex
  labels:
    app: ms-db-kardex
spec:
  ports:
  - port: 3306
    targetPort: 3306
  selector:
    app: ms-db-kardex
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-db-kardex
  labels:
    app: ms-db-kardex
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-db-kardex
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ms-db-kardex
    spec:
      containers:
      - image: mysql:8.0
        name: ms-db-kardex
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-root-credentials
              key: password
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: mysql-user-credentials
              key: username
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-user-credentials
              key: password
        - name: MYSQL_DATABASE
          valueFrom:
            configMapKeyRef:
              name: inventory-db
              key: name
        ports:
        - containerPort: 3306
          name: ms-db-kardex
        volumeMounts:
        - name: ms-db-kardex-persistent-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: ms-db-kardex-persistent-storage
        persistentVolumeClaim:
          claimName: ms-db-kardex-pv-claim
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ms-db-kardex-pv-claim
  labels:
    app: ms-db-kardex
    tier: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi