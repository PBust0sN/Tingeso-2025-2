apiVersion: v1
kind: Service
metadata:
  name: ms-db-loans
  labels:
    app: ms-db-loans
spec:
  ports:
  - port: 3306
    targetPort: 3306
  selector:
    app: ms-db-loans
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-db-loans
  labels:
    app: ms-db-loans
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-db-loans
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ms-db-loans
    spec:
      containers:
      - image: mysql:8.0
        name: ms-db-loans
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-root-credentials
              key: password
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: mysql-user-credentials
              key: username
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-user-credentials
              key: password
        - name: MYSQL_DATABASE
          valueFrom:
            configMapKeyRef:
              name: loans-db
              key: name
        ports:
        - containerPort: 3306
          name: ms-db-loans
        volumeMounts:
        - name: ms-db-loans-persistent-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: ms-db-loans-persistent-storage
        persistentVolumeClaim:
          claimName: ms-db-loans-pv-claim
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ms-db-loans-pv-claim
  labels:
    app: ms-db-loans
    tier: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi