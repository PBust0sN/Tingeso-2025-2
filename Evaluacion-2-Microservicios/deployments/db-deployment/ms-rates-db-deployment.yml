apiVersion: v1
kind: Service
metadata:
  name: ms-db-rates
  labels:
    app: ms-db-rates
spec:
  ports:
  - port: 3306
    targetPort: 3306
  selector:
    app: ms-db-rates
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-db-rates
  labels:
    app: ms-db-rates
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-db-rates
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ms-db-rates
    spec:
      containers:
      - image: mysql:8.0
        name: ms-db-rates
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-root-credentials
              key: password
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: mysql-user-credentials
              key: username
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-user-credentials
              key: password
        - name: MYSQL_DATABASE
          valueFrom:
            configMapKeyRef:
              name: rates-db
              key: name
        ports:
        - containerPort: 3306
          name: ms-db-rates
        volumeMounts:
        - name: ms-db-rates-persistent-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: ms-db-rates-persistent-storage
        persistentVolumeClaim:
          claimName: ms-db-rates-pv-claim
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ms-db-rates-pv-claim
  labels:
    app: ms-db-rates
    tier: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Mi