apiVersion: v1
kind: Service
metadata:
  name: ms-db-reports
  labels:
    app: ms-db-reports
spec:
  ports:
  - port: 3306
    targetPort: 3306
  selector:
    app: ms-db-reports
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-db-reports
  labels:
    app: ms-db-reports
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-db-reports
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ms-db-reports
    spec:
      containers:
      - image: mysql:8.0
        name: ms-db-reports
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-root-credentials
              key: password
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: mysql-user-credentials
              key: username
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-user-credentials
              key: password
        - name: MYSQL_DATABASE
          valueFrom:
            configMapKeyRef:
              name: reports-db
              key: name
        ports:
        - containerPort: 3306
          name: ms-db-reports
        volumeMounts:
        - name: ms-db-reports-persistent-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: ms-db-reports-persistent-storage
        persistentVolumeClaim:
          claimName: ms-db-reports-pv-claim
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ms-db-reports-pv-claim
  labels:
    app: ms-db-reports
    tier: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Mi